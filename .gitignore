# Build output from .github/workflows/release.yml (also created locally when
# testing the packaging steps by hand).
dist/
publish/
plugin.schema.json

# Rendered patch-test cards (tools/patch_test.py output).
tools/out/
